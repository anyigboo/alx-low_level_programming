#include <stdio.h>

/**
  * main - entry point of the program
  *
  * Return:0 if no error, non-zero if error
  */
int main(void)
{
	int a = 100;

	if (a > 20)
	{
		printf("_putchar\n");
	}
	else
	{
		printf("a is not less than 100");
	}
	return (0);
}
